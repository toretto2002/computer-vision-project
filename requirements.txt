# =============================================================
# requirements.txt — Dipendenze progetto Plant Disease CV
# Corso: Introduction to Computer Vision — Epicode
# Compatibile con Google Colab (molte già pre-installate)
# =============================================================

# --- Computer Vision ---
opencv-python>=4.8.0
scikit-image>=0.21.0          # per HOG features

# --- Deep Learning ---
torch>=2.0.0
torchvision>=0.15.0           # per ResNet18 e transforms

# --- Machine Learning classico ---
scikit-learn>=1.3.0           # per SVM, metriche, confusion matrix
joblib>=1.3.0                 # per salvare/caricare modello SVM

# --- Dati e calcolo ---
numpy>=1.24.0
pandas>=2.0.0                 # utile per tabelle risultati

# --- Visualizzazione ---
matplotlib>=3.7.0
seaborn>=0.12.0               # per heatmap confusion matrix

# --- Utility ---
tqdm>=4.65.0                  # barre di avanzamento in load_dataset
Pillow>=9.5.0                 # gestione immagini (usata da torchvision)

# --- Kaggle (solo per download dataset ) ---
kaggle>=1.5.16